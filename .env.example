# This is a convenience env example for local non-docker runs.
# Each app also has its own .env.example in /backend and /frontend.

MONGODB_URI=mongodb://localhost:27017/mandirledger
PORT=5000
CORS_ORIGIN=http://localhost:3000
TEMPLE_NAME=Shri Mandir

NEXT_PUBLIC_API_BASE_URL=http://localhost:5000
NEXT_PUBLIC_TEMPLE_NAME=Shri Mandir
